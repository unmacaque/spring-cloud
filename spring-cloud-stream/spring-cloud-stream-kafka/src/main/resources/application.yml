spring:
  cloud:
    stream:
      bindings:
        input:
          destination: unfiltered
          group: group1
        output:
          destination: filtered

logging:
  level:
    org.apache.kafka.clients: warn
