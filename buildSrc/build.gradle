plugins {
    id 'groovy-gradle-plugin'
}

dependencies {
    implementation buildLibs.springBoot
    implementation buildLibs.dependencyManagement
    implementation buildLibs.springCloudContract
    implementation buildLibs.openrewrite
}

repositories {
    mavenLocal()
    gradlePluginPortal()
}
